# secrets — never commit
.clawd-calendar.env
*.env

# runtime data (sqlite db)
data/

# personal static assets (avatar, favicons, og card) — served as an overlay
# on top of public/, never committed
assets/

node_modules/
.DS_Store
